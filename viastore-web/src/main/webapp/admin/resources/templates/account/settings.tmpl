<div class="account-settings">
    <div class="left">
        <div class="avatar">
           {{#if avatar}}
            <img src="avatar"/>
           {{/if}}
        </div>
        <div class="editable-menu">

        </div>
        <div class="tab-settings">

        </div>
    </div>
    <div class="right">
       <form data-form="form-{{id}}">
           <div class="row" data-row="row-{{id}}">
               <p data-id="name-{{id}}"></p>
               <input id="name-{{id}}" name="name-{{id}}" type="text" value="" placeholder="Name"/>
           </div>
       </form>
    </div>
</div>